def solution(left, right):
    answer = 0
    for i in range(left,right+1):
        k = 1
        count = 1
        while k <= i//2:
            if i % k == 0:
                count += 1
            k += 1
            
        print(i,count)
        
        if count % 2 == 0:
            answer += i
        else:
            answer -= i
    

    return answer
